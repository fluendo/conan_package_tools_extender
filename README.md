## Conan package tools extender

Set of classes for rapid development of Conan packages using projects based in Meson, make,
cmake or Visual Studio used in the Fluendo conan project


## Installation
Use pip to install:

`pip install cpte`
